from __future__ import annotations


def get_whale_events(api_key: str = "") -> list[dict]:
    if not api_key:
        return []
    # Whale Alert API stub — implement when key available
    return []
